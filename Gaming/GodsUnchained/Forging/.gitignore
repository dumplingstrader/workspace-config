# Secrets — never commit these
.env

# Local caches / tokens / state (regenerated at runtime)
gu_token_cache.json
token_proto_cache.json
floor_cache.json
settings.json

# Logs & screenshots
forge_logs/
screenshots/

# Python
__pycache__/
*.pyc
.venv/
venv/
